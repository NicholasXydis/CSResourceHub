{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CS Resource Hub",
  "description": "Schema for a single resource entry",
  "type": "object",
  "required": [
    "id",
    "name",
    "url",
    "description",
    "category",
    "status",
    "location",
    "last_verified"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100
    },
    "url": {
      "type": "string",
      "pattern": "^https://.+"
    },
    "description": {
      "type": "string",
      "minLength": 10,
      "maxLength": 120
    },
    "category": {
      "type": "string",
      "enum": [
        "learning-resources",
        "communities",
        "clubs",
        "volunteer",
        "ctfs",
        "competitions",
        "hackathons",
        "game-jams",
        "projects",
        "repositories",
        "open-source-help",
        "research",
        "conferences",
        "fellowships",
        "online-events",
        "career-fairs",
        "scholarships",
        "startup-programs",
        "freelance",
        "certifications",
        "free-benefits"
      ]
    },
    "cost": {
      "type": "string",
      "enum": ["free", "freemium", "paid"]
    },
    "status": {
      "type": "string",
      "enum": ["active", "paused", "archived"]
    },
    "location": {
      "type": "string",
      "minLength": 2,
      "maxLength": 120
    },
    "month": {
      "type": "string",
      "enum": [
        "January",
        "February",
        "March",
        "April",
        "May",
        "June",
        "July",
        "August",
        "September",
        "October",
        "November",
        "December"
      ]
    },
    "date_added": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    },
    "last_verified": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    }
  }
}
